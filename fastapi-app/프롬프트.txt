모든 코드를 읽고 json schema 부터 작성하자
  json schema가 쓰이는 부분은 llm에게 전달되는 프롬프트 와
  프롬프트용 예시
  이후 가져온 값을 바탕으로 pptx를 만들 때야
  둘의 차이점이 있을 수 있음
  일단 llm 프롬프트 기준이며 좀 더 확장성이 좋은 것이 있다
  면 그것은 추가 고려로 빼줘
  대상은 slots와 elements 에 대해서 진행
  각각 파일로 만들어서 fastapi-app 폴더 밑에 slots.json
  elements.json으로 작성해줘

  작성 파일은 설명 파일이야 주석 같은거 있어도 좋아 llm에게
  프롬프트 용으로 사용할 것임으로 문법을 모두 지킬 필요 없

---

일단 코드 수정하자 llm 프롬프트 수정
  사전 작업 : 아까 만든 json들 띄여쓰기 들여쓰기 등등 줄여서 필요 없는 글자 없애줘
  작업 1 : 항상 붙이는 버전으로 llm에게 가는 프롬프트 수정해, 모든 llm 호출에서의 프롬프트 확인해줘, 프롬프트 변수값 잘 설정해줘
  작업 2 : json_validation.py validate 하는 로직 확인해줘 혹시 json 변경되면서 수정 필요한지 확인하고 수정해줘
  작업 3 : pptx를 만드는 부분 json에 있는 값들 모두 처리 가능한지 확인하고 부족한 부분 있으면 추가 구현, 필요 없어진 부분 있으면 제거
  사후 작업 : 모든 llm 호출 할 때 프롬프트 값, 반환 받은 값 싹다 log로 출력해줘, 이전 로그 지우로 다시 해줘, 어떤 부분에서 호출인지도 알려줘 (outline or
  slide 1번, ...)