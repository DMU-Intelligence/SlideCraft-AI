/**
 * PostCSS Configuration
 *
 * Tailwind CSS v4 (via @tailwindcss/postcss) wires the required plugins.
 * This file exists for adding additional PostCSS plugins if needed.
 */
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
